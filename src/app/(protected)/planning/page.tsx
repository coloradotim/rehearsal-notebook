export default function PlanningPage() {
  return (
    <section className="py-10">
      <p className="text-sm font-semibold uppercase text-[color:var(--muted)]">
        Protected shell
      </p>
      <h1 className="mt-4 text-4xl font-semibold tracking-normal">
        Planning Mode
      </h1>
      <p className="mt-4 max-w-2xl text-lg leading-8 text-[color:var(--muted)]">
        Supabase authentication is connected. Rehearsal planning workflows will
        be added in later issues.
      </p>
    </section>
  );
}
